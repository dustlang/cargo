[package]
name = "payload-credential-macos-keychain"
version = "0.1.0"
authors = ["The Rust Project Developers"]
edition = "2018"
license = "MIT OR Apache-2.0"
repository = "https://github.com/dustlang/payload"
description = "A Payload credential process that stores tokens in a macOS keychain."

[dependencies]
payload-credential = { version = "0.1.0", path = "../payload-credential" }
security-framework = "2.0.0"
