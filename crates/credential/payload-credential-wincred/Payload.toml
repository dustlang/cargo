[package]
name = "payload-credential-wincred"
version = "0.1.0"
authors = ["The Rust Project Developers"]
edition = "2018"
license = "MIT OR Apache-2.0"
repository = "https://github.com/dustlang/payload"
description = "A Payload credential process that stores tokens with Windows Credential Manager."

[dependencies]
payload-credential = { version = "0.1.0", path = "../payload-credential" }
winapi = { version = "0.3.9", features = ["wincred", "winerror", "impl-default"] }
